% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{reshape_ref_data}
\alias{reshape_ref_data}
\title{Reshape a data stream into a full source-proportion matrix}
\usage{
reshape_ref_data(data_streams_list, target = "df_stream_2", order_ref)
}
\arguments{
\item{data_streams_list}{A named list containing \code{df_stream_1} and
\code{df_stream_2}. \code{df_stream_1} must contain stream-1 tracer observations with
one column per tracer and may contain a \code{group} column. \code{df_stream_2} must
contain one column per sampled source with values interpreted as stream-2
mixing proportions and may contain a \code{group} column. An \code{Unsampled} column
is optional; if absent, the unsampled component is reconstructed as
\verb{1 - rowSums(sampled proportions)}.}

\item{target}{The target element of \code{data_streams_list}, typically
\code{"df_stream_2"}.}

\item{order_ref}{A character vector specifying the correct order of the
source names.}
}
\value{
A matrix of N x (J + 1), with the + 1 representing the
mixing proportions from the unsampled source(s).
}
\description{
Extracts the sampled-source proportions for \code{target} from
\code{data_streams_list}, in the column order given by \code{order_ref}, and appends
the reconstructed unsampled-source proportion
(\verb{1 - rowSums(sampled proportions)}).
}
\examples{
library(MixMustR)
reshape_ref_data(synthetic_df_convergent, target = "df_stream_2",
                 order_ref = tracer_parameters$mus$source)

}
